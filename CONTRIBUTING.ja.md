# 一緒に作る

[한국어](CONTRIBUTING.md) | [English](CONTRIBUTING.en.md) | **日本語**

## はじめに

[SAFETY.md](SAFETY.md) を読んでください。ここに書かれた規則は機能より優先します。
破る理由が見つかったなら、それはコードより先に
[ADR](docs/decisions/README.md) に書く話です。

## 動かす

```bash
pnpm install
pnpm app
```

詳しくは [ローカルで動かす](docs/guides/running-locally.md) に書いてあります。

## どこを触ればいいか

| やりたいこと | 読むもの |
|---|---|
| 検出ルールを足す | [検出器を足す](docs/guides/adding-a-detector.md) |
| MCP ツールを足す | [エージェントをつなぐ](docs/guides/connecting-agents.md) |
| 復元まわりを触る | [ブランチを戻す](docs/guides/restoring-branches.md) |
| 画面の文言、言語を足す | [言語を足す](docs/guides/adding-a-language.md) |
| 全体の構成 | [構造](docs/architecture.md) |

## コード

**コメントには「なぜ」を書きます。**「何をしているか」はコードが言います。
そうした理由、とくに **別のやり方で失敗した経験**を残してください。
このリポジトリのコメントはほとんどがその記録です。

**読んでわかる名前を。** `s`, `c`, `f` ではなく `stats`, `caseFile`, `finding` と書きます。

**300 行を超えたら**分ける場所を探してください。
行数そのものが問題なのではなく、1 つのファイルが複数の仕事をしている合図と見ます。

**依存の向きは一方通行です。** `ui` → `core` → `platform` → `server`。
`core` の中から `../ui` を読み込むとレビューで止まります。

## 文章

画面に出る文字列はコードに直接書かず、`src/i18n/locales/` に入れます。
한국어, English, 日本語 の 3 つが揃っていないとビルドが通りません。

README も 3 つ、この文書も 3 つあります。
GitHub は読む人の言語で選んでくれないので、1 行目で互いにリンクしています。
中身を直すときは 3 つとも直してください。抜けは `pnpm style` が拾います。

以下は画面の文言にもコメントにも当てはまります。

- 短く書く
- 二重否定を使わない
- 長いダッシュ (—), 中黒 (·), 三点リーダ (…) を使わない。`-`, `,`, `...` で書きます
- 実在の組織名やリポジトリ名を残さない
- **「安全です」と書かない。** わかっているのは「信号が出なかった」であって「ない」ではありません

## Issue

[様式が 4 つ](https://github.com/gitmoru/gitmoru/issues/new/choose) あります。

| 様式 | どんなとき |
|---|---|
| It showed nothing, but something was there | 画面は静かだったのに、実際にはやられていた。**これが一番ほしい報告です** |
| Something broke | 落ちる、画面が真っ白、数が合わない |
| A signal worth catching | 思いつきでも構いませんが、実際の出来事が付いているとずっと良いです |
| The wording reads wrong | 3 つの言語のどれかが、その言語らしく読めないとき |

様式は英語ですが、**本文は書きやすい言語でどうぞ。** 英語なのは項目名だけです。

**書かないでください。**

- 実在の組織名、リポジトリ名。`someorg/somerepo` で十分です
- まだ動くペイロード。形だけ書くか、URL と鍵を消して貼ってください
- ログの中のトークン。このツールのログには **あなたの GitHub トークン**が入ります

空の Issue は閉じてあります。この 3 行はどの様式にも先頭に書いてあるのですが、
空の Issue で開くと誰も読まないまま開いてしまうからです。

gitmoru **そのもの**の穴は Issue ではなく [SECURITY.md](SECURITY.md) へお願いします。

## プルリクエストの題名がバージョンを決めます

conventional commits を使います。好みの問題ではありません。
マージすると **題名がそのままコミットになり**、そのコミットを見て次のバージョンが決まります。

| 題名 | 何が起きるか |
|---|---|
| `fix: ...` | 0.1.0 → 0.1.1 |
| `feat: ...` | 0.1.0 → 0.2.0 |
| `feat!: ...` | 0.1.0 → 1.0.0。何が壊れるかを本文に書いてください |
| `docs:` `refactor:` `test:` `build:` `chore:` | 何も出ません |

**英語で、小文字で、1 文、句点なし。** 1 本に 1 つのことだけ入れてください。

```
feat: add blob hash comparison to the detector
fix: stop the first screen from swallowing clicks
docs: write down how to add an MCP tool
```

バージョンは手で上げません。`feat:` か `fix:` が入るとロボットが
「バージョンを上げる」プルリクエストを開き、人がそれをマージすると npm に出ます。
つまり **題名を雑に書くと、出るはずのものが出ません。**

## テスト

純粋なロジックにはテストを付けます。`src/core/__tests__/` にあります。

```bash
pnpm test
pnpm test:watch
```

ネットワークを使うテストは作りません。GitHub の呼び出しは偽物で置き換えます。
他人のリポジトリを触るテストは CI で回せません。

**ここにある例のほとんどは、実際に間違えたものです。**
新しい規則を書いて「これは当然通るはず」と思ったら、そのときがテストを書くときです。
このリポジトリではその勘が 3 回外れました。

## プルリクエストを開く前に

```bash
pnpm check
```

型、文章の規則、テスト、ドットの動き、ビルドをまとめて見ます。CI も同じものを回します。

様式の最後に **SAFETY.md をまたぐか**を聞く欄があります。
何も付いていないのが普通で、空欄も答えです。飛ばした印ではありません。
1 つでも付けたなら、それを許す ADR を貼ってください。ADR がないなら、それが先に出すプルリクエストです。

復元やプロキシを触ったなら、何を試したか必ず書いてください。
この 2 つは間違えると他人のリポジトリが飛びます。
